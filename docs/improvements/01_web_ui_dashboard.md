# Web UI Dashboard

**Rank:** Highest

Right now, the interface relies entirely on the terminal and HTML static file outputs. A fantastic architectural step forward would be to serve a simple local web app (using FastAPI or Streamlit). This dashboard could show today's selected videos, history of vocabulary learned, and provide an interface to instantly trigger `--force-fetch`.
